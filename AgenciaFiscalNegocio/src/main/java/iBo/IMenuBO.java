package iBo;

/**
 * Interfaz que define un método para verificar la existencia de entidades en el sistema.
 * 
 * <p>Esta interfaz proporciona un método para verificar si existen entidades en el sistema.</p>
 * 
 * @author asielapodaca
 */
public interface IMenuBO {
    /**
     * Verifica la existencia de entidades en el sistema.
     * 
     * @return true si existen entidades en el sistema, false en caso contrario.
     */
    public boolean verificarExistenciaDeEntidades();
}
